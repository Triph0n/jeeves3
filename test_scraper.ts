import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch({ headless: true }); const page = await browser.newPage(); await page.goto('https://www.musikzeitung.ch/stellen/?_sf_s=cello', { waitUntil: 'networkidle2' }); await page.waitForSelector('h3 a', { timeout: 5000 }).catch(e=>console.log('Timeout')); const res = await page.evaluate(() => document.querySelectorAll('h3 a').length); console.log('Found Links:', res); await browser.close(); })();
